# Pack Schemas

Schemas validate structure, not all semantic rules. The importer must also verify stable-ID relationships, supported block/activity types, dependency presence, version rules and safety limits.

The initial included schema is deliberately small and supports the first vertical slice. Expand it through versioned changes and fixtures.
