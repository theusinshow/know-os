# Importer Fixtures

Add fixtures as the importer is implemented:

- valid minimal Pack;
- invalid JSON;
- invalid schema;
- unsupported schema version;
- same ID/same version/same content;
- same ID/same version/different content conflict;
- newer version update;
- missing concept dependency;
- oversized/deep payload;
- transaction rollback case.
