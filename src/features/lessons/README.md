# Lessons

Lesson navigation, block rendering and lesson application services live here.

- `blocks/` contains the allowlisted lesson block renderer registry. Packs never provide arbitrary React/UI payloads.
